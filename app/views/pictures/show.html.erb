<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @picture.name %>
</p>

<p>
  <%= image_tag(@picture.url, :class => "thumbnail")%>
</p>

<p>
  <strong>User:</strong>
  <%= @picture.user ? @picture.user.username : "Anonymous" %>
</p>

<p>
  <strong>Rating:</strong>
  <%= @picture.rating %>
</p>

<%= link_to 'Edit', edit_category_picture_path(@category, @picture) %> |
<%= link_to 'Back', compare_category_path(@category) %>
